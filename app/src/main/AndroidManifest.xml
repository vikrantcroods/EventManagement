<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.croods.eventmanagement">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/download"
        android:label="Prageu"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/download"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoActionBar"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="n">
        <activity android:name=".activity.ActiveEventActivity"></activity>
        <activity android:name=".activity.CreateCustomerActivity" />
        <activity android:name=".activity.ProductReceivedActivity" />
        <activity
            android:name=".activity.ReceivedToWarehouseActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".activity.EventCreateActivity" />
        <activity
            android:name=".activity.ReceivedToEventActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".activity.AddProdcutActivity" />
        <activity android:name=".activity.BarcodeScannerActivity" />
        <activity
            android:name=".activity.SendMaterialActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".activity.CustomerDetailActivity" />
        <activity
            android:name=".activity.DashBoardActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>